h1. Installation

bq. git clone git://github.com/petemcw/Dotfiles.git ~/.dotfiles
cd ~/.dotfiles
rake install

h1. Environment

This repository is mainly setup for @zsh@.  If you would like to switch to zsh, you can do so with the following command.

bq. chsh -s /bin/zsh
