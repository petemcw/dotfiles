# Executes commands at login pre-zshrc.

# Ensure Homebrew is added to PATH.
eval "$(/opt/homebrew/bin/brew shellenv)"

# Ensure Homebrew completions are added to FPATH.
FPATH="${HOME}/.dotfiles/src/zsh/custom/completions:$(brew --prefix)/share/zsh/site-functions:${FPATH}"
